# Sample `.envrc` file
# Comments can be added using the '#' symbol

# Public-facing variable might exist here
export PROJECT_NAME="valkyrie-project-template"
export SERVICE_API_KEY="your-api-key"
export DEBUG=true

# Private, non-exported variables here
SERVICE_SECRET_KEY="your-secret-key"
